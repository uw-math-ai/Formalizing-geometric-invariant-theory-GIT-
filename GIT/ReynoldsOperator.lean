import GIT.Reynolds.LocallyFiniteActions
import GIT.Reynolds.LinearReductivity
import GIT.Reynolds.FiniteDimensionalReynolds
import GIT.Reynolds.LocallyFiniteReynolds
import GIT.Reynolds.AlgebraReynolds

/-!
# The Reynolds operator for linearly reductive groups (umbrella file)

This file is a thin re-export of the modular `GIT/Reynolds/` hierarchy. Importing
`GIT.ReynoldsOperator` makes the full Reynolds-operator development available in one go,
matching the old monolithic interface.

For the per-file breakdown, the dependency graph, and the proof-chain index, see
`GIT/Reynolds/README.md`.
-/
